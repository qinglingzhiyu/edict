# 现代技术部门 Multi-Agent 协作系统产品需求文档 (PRD)

## 1. 背景与目标
本项目旨在将原有的“三省六部”古代帝国制 Multi-Agent 协作框架，改造为**现代互联网技术部门**的工作流。
通过引入 7 个典型的技术岗位 Agent（PMO、产品、UI、前端、后端、测试、运维），模拟真实的软件研发全生命周期，实现从“需求输入 -> 规划 -> 设计 -> 开发 -> 测试 -> 上线”的全自动化与透明化流转。

## 2. 角色定义与职责 (Agents)

系统共包含 7 个核心 Agent：

| 岗位 (Agent ID) | 职责说明 | 工作流位置 | 对标原角色 |
| --- | --- | --- | --- |
| **PMO (pmo)** | **需求入口与整体把控**。负责接收外部需求（如业务方、用户的原始输入），进行初步分拣、项目建档，并分配给产品经理。 | 起点 / 终点 | 太子 (taizi) |
| **产品经理 (product)** | **需求分析与规划**。接收 PMO 派发的需求，进行需求拆解，输出详细的 PRD (产品需求文档)，并协调后续设计与开发资源。 | 规划节点 | 中书省 (zhongshu) |
| **UI 设计师 (ui)** | **界面与交互设计**。根据产品经理的 PRD，输出界面设计稿和交互规范，交付给前端工程师。 | 设计节点 | （新设/部分门下职责） |
| **前端工程师 (frontend)** | **页面开发**。依据 PRD 和 UI 设计稿，进行前端页面的开发与逻辑实现。与后端进行接口对接。 | 执行节点 | 兵部/工部 |
| **后端工程师 (backend)** | **接口与架构开发**。依据 PRD，进行数据库设计、业务逻辑开发、API 接口提供。 | 执行节点 | 户部/兵部 |
| **测试工程师 (qa)** | **质量把控与联调测试**。在前后端开发完成后介入，进行系统测试。发现 Bug 则打回给开发修改；测试通过则出具测试报告。 | 审核/质量节点 | 刑部/门下省 |
| **运维工程师 (ops)** | **部署与发布上线**。接收测试通过的代码产物，负责生产环境的部署、发布上线，并将结果反馈给 PMO。 | 终点执行 | 工部/尚书省 |

## 3. 工作流与状态机设计 (Workflow & State Machine)

原有的 `(太子分拣 -> 中书规划 -> 门下审议 -> 已派发 -> 执行中 -> 待审查 -> 已完成)` 状态链将变更为符合现代敏捷开发的阶段。

### 3.1 核心任务状态 (Task States)
1. **需求池 (Backlog)**: 需求已接收，PMO 正在处理或排期。
2. **产品规划 (Planning)**: 产品经理正在撰写 PRD 和拆解子任务。
3. **设计中 (Designing)**: UI 设计师正在出图。
4. **开发中 (Developing)**: 前端和后端正在并行开发。
5. **测试联调 (Testing)**: 测试工程师正在进行验证。若有 Bug，则状态可流转回 `Developing`。
6. **待发布 (ReadyForRelease)**: 测试通过，交由运维准备发布。
7. **已上线 (Released)**: 运维发布成功，任务闭环。

### 3.2 权限与通信矩阵 (Visibility & Messaging)
- **PMO** 可以与 **业务方(用户)** 和 **产品经理** 直接沟通。
- **产品经理** 可以与 **PMO**、**UI**、**前端**、**后端** 沟通。
- **UI** 只能与 **产品经理** 和 **前端** 沟通。
- **前端** 与 **后端** 可以相互沟通（联调 API）。
- **测试** 可以与 **前端**、**后端**、**运维** 沟通。
- **运维** 只能接收来自 **测试** 的发布指令，并向 **PMO** 汇报结果。

## 4. 系统改造拆解 (Implementation Plan)

### 4.1 配置文件重构
1. 删除 `agents/` 下原有的古代角色，新建 `pmo`, `product`, `ui`, `frontend`, `backend`, `qa`, `ops` 这 7 个目录，并为每个角色编写专属的 `SOUL.md`（Prompt 设定和工作协议）。
2. 修改 `install.sh` 中的 Agent 注册逻辑，重新配置 `openclaw.json` 的 `allowAgents` 权限矩阵。

### 4.2 后端与状态机改造 (Python Scripts)
1. 重点改造 `scripts/kanban_update.py`：
   - 更新 `_VALID_STATES` 列表为上述敏捷开发状态。
   - 更新 `_DEPARTMENTS` 为 7 个新岗位的名称。
   - 修改状态流转的合法性校验逻辑（如：`Testing` 状态可以回到 `Developing`，但不能直接到 `Released`）。
2. 修改其他依赖硬编码部门名称的脚本（如 `sync_officials_stats.py`, `dashboard/server.py` 等）。

### 4.3 前端看板改造 (UI & Dashboard)
1. **词汇现代化**：
   - “皇上” -> “需求方/业务”
   - “圣旨” -> “需求”
   - “奏折阁” -> “项目归档”
   - “上朝仪式” -> “每日站会 (Daily Standup)”
   - “军机处” -> “研发看板”
2. **看板列调整**：将 Kanban 组件的列变更为 Backlog, Planning, Designing, Developing, Testing, Released。
3. **样式与图标**：替换古风图标为现代科技感图标（如 Laptop, Code, Bug, Rocket 等）。
4. **模板库**：将预设模板变更为“新增功能”、“体验优化”、“紧急 Bug 修复”、“技术重构”等。

## 5. 预期效果
改造完成后，用户只需在对话中输入一句：“我要做一个电商的购物车功能，支持满减优惠”，系统将自动触发：
`PMO 接单立项 -> 产品出具购物车 PRD -> UI 设计界面 -> 前端写页面 / 后端写接口 -> QA 测试验证 -> 运维发版上线` 的全流程无缝协作。
